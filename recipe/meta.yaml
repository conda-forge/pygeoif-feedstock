{% set name = "pygeoif" %}
{% set version = "1.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pygeoif-{{ version }}.tar.gz
  sha256: 924dfdc8c6afb5ea71015129767354c7bf50d10811a98a9b2f44d071ca2d6ba4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - typing-extensions

test:
  imports:
    - pygeoif
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/cleder/pygeoif/
  summary: A basic implementation of the __geo_interface__
  license: LGPL-2.1-or-later
  license_file:
    - LICENSE.GPL
    - LICENSE.txt

extra:
  recipe-maintainers:
    - benjwadams
    - daf
    - kwilcox
    - lukecampbell
    - ocefpaf
